# 前后端本地联调

## 启动服务

在两个终端中分别从仓库根目录执行。

后端（端口 8000）：

```sh
cd backend
uv sync --locked
uv run uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

前端：

```sh
cd frontend
npm install
npm run dev
```

浏览器打开 Vite 终端显示的地址，默认是 `http://localhost:5173`。

## 请求转发

Vite 将 `/api` 及 `/api/` 下的请求转发到 `http://127.0.0.1:8000`，保留原路径，不移除 `/api`。

```text
浏览器请求前端地址的 /api/health
→ Vite 转发到 http://127.0.0.1:8000/api/health
→ FastAPI 返回 {"status":"ok"}
```

配置位于 [frontend/vite.config.ts](frontend/vite.config.ts)。
浏览器请求的仍是前端同源地址，不需要为此在 FastAPI 开放跨域权限。
不要将前端请求写成硬编码的 `http://127.0.0.1:8000/...`，那样会绕过代理。

## 验证

在前端页面的浏览器控制台执行：

```js
const response = await fetch('/api/health');
console.log(response.status, await response.json());
// 200 { status: 'ok' }
```

也可以直接访问后端：

- 健康检查：<http://127.0.0.1:8000/api/health>。
- 接口文档：<http://127.0.0.1:8000/docs>。

直接访问后端只验证后端本身；从前端地址请求 `/api/health` 才会经过 Vite 代理。

## 注意事项

- 后端未启动时代理请求会失败，不是前端路由的 404。
- 修改 Vite 配置后建议重启开发服务。
- 如果 8000 端口被占用，先确认是否已有本项目的后端运行，不要重复启动。
- 此配置用于本地 Vite 服务，不会打包成生产代理。正式部署时需另行安排 `/api` 转发。

配置依据：[Vite server.proxy](https://vite.dev/config/server-options#server-proxy)。

## 登录接口联调

前端顶部已提供“登录”入口，登录页面为 `/login`。默认 `AUTH_ENABLED=false`，访问登录接口返回 503，顶部会显示“登录状态暂不可用”，不影响练习和健康检查。

开启后手动验证：发送验证码 → 登录成功返回练习库 → 刷新仍显示“已登录” → 退出 → 刷新后显示“登录”。修改手机号会清空已保存的请求 ID 和验证码；发送倒计时仅是前端提示，最终限制由后端执行。离开或刷新登录页后，未完成的表单不保留。

`/me` 只有返回 401 才显示未登录；网络故障或 503 显示“登录状态暂不可用”，可点击“重试”重新查询。退出请求失败也不会直接当作已退出。练习不要求登录，本地自定义练习仍未做账号隔离，切换账号不会自动迁移、清除或隔离这些数据。

本地需要测试登录时，在已有的 `backend/.env` 中补充以下项，不要覆盖数据库和短信配置，也不要提交该文件：

```dotenv
AUTH_ENABLED=true
AUTH_COOKIE_SECURE=false
AUTH_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:8000
```

来源是**浏览器正在访问的页面地址的协议、主机、端口**，不是代理的目标地址。Vite 换端口后需同步修改配置并重启后端；使用 `localhost:8000/docs` 时也需显式加入该来源。线上改为实际 HTTPS 来源，保留 `AUTH_COOKIE_SECURE=true`，不保留本地来源。

先应用迁移，再加载配置启动后端（从 `backend/` 执行）：

```sh
uv run --locked --env-file .env alembic upgrade head
uv run --locked --env-file .env uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

启动本身不发送短信。**手动调用发送接口会发送真实短信并可能产生费用**；仅使用自己的手机号，不自动重试。自动化测试使用模拟服务，可先执行 `uv run --locked --no-env-file pytest` 验证完整流程。

| 接口 | 请求 JSON | 成功响应 |
| --- | --- | --- |
| `POST /api/auth/sms-code` | `{"phone_number":"自己的11位手机号"}` | `{"request_id":"…"}`，仅表示接受发送请求 |
| `POST /api/auth/login` | `{"request_id":"上一步返回值","code":"6位验证码"}` | `{"id":用户ID}`，通过 Set-Cookie 设置凭证 |
| `GET /api/auth/me` | 无 | `{"id":用户ID}` |
| `POST /api/auth/logout` | 无 | 204，无响应体，撤销当前会话并清除 Cookie |

前端继续使用 `/api/...` 相对地址；POST JSON 设置 `Content-Type: application/json`。同源 fetch 默认携带 Cookie，不需要前端读取或另存 token。POST 的 Origin 由浏览器设置；命令行客户端测试时须明确提供允许的 Origin，并自行管理 Cookie。

响应错误统一为 `{"detail":"提示"}`：400 为验证码错误或请求不可用，401 为未登录或会话失效，403 为来源不允许，422 为参数格式错误，429 为发送受限，503 为登录关闭或服务暂不可用。错误响应不回显请求中的手机号、验证码或底层服务信息。所有这些登录接口响应都设为 `Cache-Control: no-store`。

Cookie 设置参考 [FastAPI Response Cookies](https://fastapi.tiangolo.com/advanced/response-cookies/)；来源保护参考 [OWASP CSRF Prevention](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)。当前实现只支持同源前端，不开放凭证跨域权限；公网开放前还需补齐 IP 限流及短信预算。
