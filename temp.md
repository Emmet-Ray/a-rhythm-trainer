看完这份清单，我建议你下一大块做 **“有规则的随机练习”**。不是再加一个简单的随机函数，而是把它作为一个完整模块：**用户选择想练的内容 → 生成符合条件的题目 → 练习 → 再来一道。**

后端可以作为再下一阶段，但最好由“跨设备保存、管理自己的练习”等具体需求带出来，不必因为前端已经做了很多，就立刻转向后端。

### 为什么优先随机练习

你现在已经有两种可用的训练方式，也有预设题目、自定义编辑和本地保存。最值得利用的是：**谱面、播放、编辑、判定这些基础能力已经可以承接不同的题目来源。**

随机练习正好利用这些能力，同时补上一个目前仍缺少的核心价值：用户不必一直练固定题目，也不必自己出题。

相比之下：

| 方向                   | 主要收益                         | 我对当前优先级的看法                             |
| ---------------------- | -------------------------------- | ------------------------------------------------ |
| 随机生成规则           | 提供不断变化、符合练习目标的题目 | 最适合下一大块                                   |
| 后端与数据库           | 持久管理、跨设备访问用户内容     | 适合由明确的保存需求驱动                         |
| 几何节奏游戏           | 补齐第三种核心训练方式           | 值得独立探索，但交互设计的不确定性更大           |
| 听写题目设计           | 补充有学习顺序的正式内容         | 重要，但可以单独安排，不必阻塞功能开发           |
| 编辑器、音色、布局优化 | 改善现有体验                     | 按实际使用中遇到的问题逐项做，不必打包成一次大改 |

### 第一大块：把随机练习做成“可控出题”

这里最重要的不是随机程度，而是**生成结果是否符合用户想练的东西**。

例如“练八分音符”，有两个不同的含义：

- 允许出现八分音符，但这次可能全是四分音符。
- 每道题都包含八分节奏，确保用户确实练到它。

这类约定才是生成规则的核心。

建议分成三步：

1. **先确定第一版规则的含义。**  
   支持哪些节奏型、多少小节、是否允许休止符、哪些内容必须出现。先做少数清楚的规则，不急着开放所有参数。

2. **实现确定可验证的生成器。**  
   用节奏型组合生成完整小节，保证时值合法；测试不仅检查四拍，还检查题目确实符合规则。可以让测试传入可控制的随机来源，方便复现问题。

3. **完成用户的连续练习流程。**  
   选择规则、生成、练习、再生成。明确“调整规则只影响下一题”“生成新题会结束旧的一轮”等行为。

**这一阶段的完成标准：用户能够围绕一种节奏内容，持续得到不同且合格的题目。** 不需要同时引入 AI、复杂难度系统或新的拍号。

### 第二大块：以“我的练习”为目标接入后端

如果接下来要学后端，我建议选择一个边界很清楚的目标：

> 用户保存的自定义练习，不再只存在某个浏览器里，而是能够可靠地管理，并在其他设备上使用。

这会自然带出后端真正需要解决的问题：

- 练习如何保存、读取、更新和删除。
- 服务端如何校验节奏数据。
- 如果支持账户，练习归谁所有、谁能访问和修改。
- 网络失败时，如何保留草稿、允许重试。
- 已有浏览器本地练习如何迁移。

先把**一种内容的完整存取流程**做通，再考虑让预设题库也从后端加载。预设题目目前写在前端并不是问题，数据库也不需要一次接管所有内容。

而声音排程、敲击时间记录和即时判定，仍然留在前端；后端不参与每一次敲击。

### 几何游戏可以作为另一个明确的分支

如果你此刻更想验证最初的第三种模式，也可以优先做一个**几何表示的小原型**：同一份节奏数据，用几何位置和运动表达，复用现有击拍判定。

但建议把“几何表示是否容易理解、是否好玩”和“配合背景音乐制作完整小游戏”分成两个阶段。后者会引入曲目、关卡编排和音乐同步等一整套新问题，范围明显更大。

另外，清单里有两项已经滞后了：**自定义练习本地保存已经完成，单元测试也已有一批，最近运行的是 99 个测试。** 后面应该持续为新规则和边界行为补测试，而不是再单独安排一个“开始加测试”的阶段。

**我的优先顺序是：可控随机练习 → 后端支持“我的练习” → 几何模式原型。** 这样每一阶段都有独立、能实际使用的成果，也不会让接下来的开发变成持续补零碎按钮。
